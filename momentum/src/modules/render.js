const WIDGETS = {
  time: {
    type: 'time',
    parentNode: '.main',
    className: 'time',
  },
  date: {
    type: 'date',
    parentNode: '.main',
    className: 'date',
  },
}

function showWidget(node) {
  const { type, parentNode, className } = widget
}

export { showWidget }
